# SPDX-FileCopyrightText: 2026 Avish Jha <avish.j@pm.me>
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions: {}

jobs:
  verify-meta:
    name: Verify Meta
    uses: ./.github/workflows/verify-meta.yml
    permissions:
      security-events: write  # passed through to _security.yml for SARIF upload

  changes:
    name: Detect Changed Stacks
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      pull-requests: read  # dorny/paths-filter reads PR file list via the API on pull_request events
    outputs:
      stacks: ${{ steps.filter.outputs.changes }}
    steps:
      - name: Setup
        uses: avishj/blueprints/.github/actions/setup@19bdb966ebc356d5b517946bc707c4d9e07f8361  # main
        with:
          uv: "false"
          just: "false"
          bun: "false"

      - name: Detect changed stacks
        id: filter
        uses: dorny/paths-filter@fbd0ab8f3e69293af611ebaee6363fc25e6d187d  # v4.0.1
        with:
          filters: |
            astro-ts-react:
              - 'stacks/astro-ts-react/**'
            python-cli:
              - 'stacks/python-cli/**'

  verify-stack:
    name: Verify Stack
    needs: changes
    if: needs.changes.outputs.stacks != '[]'
    strategy:
      fail-fast: false
      matrix:
        stack: ${{ fromJson(needs.changes.outputs.stacks) }}
    uses: ./.github/workflows/verify-stack.yml
    with:
      stack: ${{ matrix.stack }}
    secrets: inherit  # required for env-scoped SMOKE_TOKEN to resolve in the called workflow
    permissions:
      pull-requests: write  # smoke job posts rolling comment on the PR

  ci-passed:
    name: CI Passed
    if: always()
    needs: [verify-meta, changes, verify-stack]
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions: {}
    steps:
      - name: Check results
        env:
          HAS_FAILURE: ${{ contains(needs.*.result, 'failure') }}
          HAS_CANCELLED: ${{ contains(needs.*.result, 'cancelled') }}
        run: |
          if [[ "$HAS_FAILURE" == "true" || "$HAS_CANCELLED" == "true" ]]; then
            echo "::error::One or more jobs failed or were cancelled."
            exit 1
          fi
